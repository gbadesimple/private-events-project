<h1 class="title is-5"> Upcoming Events ( <%= @events.future.count %> ) </h1>
  <ol>
      <% @events.future.each do |event| %>
        <li>
          <%= link_to event.name, event_path(event[:id]) %>
        </li>
    <% end %>
  </ol> 
