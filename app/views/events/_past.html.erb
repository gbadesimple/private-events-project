<h1 class="title is-5"> Past Events ( <%= @events.past.count %> ) </h1>
  <ol>
      <% @events.past.each do |event| %>
        <li>
          <%= link_to event.name, event_path(event[:id]) %>
        </li>
    <% end %>
  </ol>
